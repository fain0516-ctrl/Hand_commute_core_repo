# Pico 2 중간 제어 보드 사양서
(상위 보드 ↔ Pico 2 + W5500 ↔ STS3215 6축 + PWM 4축)

---

## 1. 전체 역할 분담

| 계층 | 보드 | 역할 |
|------|------|------|
| 상위 | Raspberry Pi 4/5 | ROS 2, EKF, 경로계획, 어드미턴스 제어 등 고수준 로직 |
| 중간 | **Pico 2 + W5500** | 이더넷 패킷 수신 → 프로토콜 변환 → STS3215 위치 명령 + PWM 출력 |
| 하위 | STS3215 × 6 | 시리얼 버스 스마트 서보 (위치 제어) |
| 하위 | PWM 모터/서보 × 4 | 일반 PWM 제어 |

Pico 2는 실시간성 있는 저수준 제어만 담당한다.  
상위 보드는 100Hz 주기로 목표값을 보내고, Pico 2는 가능한 한 낮은 지연으로 모터에 반영한다.

---

## 2. 간단한 패킷 구조 (UDP 권장)

상위 보드 → Pico 2 로 보내는 패킷 (Little-endian)

```
Offset  Size    내용
0       2       Header (0xAA 0x55 고정)
2       1       Sequence (0~255 순환)
3       1       Command (0x01 = 위치+PWM 업데이트)
4       2       Axis0 Position (0~4095, STS3215)
6       2       Axis1 Position
8       2       Axis2 Position
10      2       Axis3 Position
12      2       Axis4 Position
14      2       Axis5 Position
16      2       PWM0 (0~1000 또는 1000~2000 us 매핑)
18      2       PWM1
20      2       PWM2
22      2       PWM3
24      2       CRC16 (또는 단순 Checksum)
```

총 길이: 26 bytes

- 주기: 10ms (100Hz) 권장
- 프로토콜: UDP (간단하고 빠름). TCP는 재전송 때문에 지연이 커질 수 있음.
- Pico 2 → 상위 보드 피드백 패킷은 나중에 필요하면 추가 (현재 위치, 전류, 에러 상태 등)

---

## 3. 핀 배분 (Pico 2)

### 이더넷 (W5500 SPI)
| Pico 2 핀 | 기능          | W5500 핀 |
|-----------|---------------|----------|
| GP16      | SPI0 RX (MISO)| MISO     |
| GP17      | SPI0 CSn      | SCS      |
| GP18      | SPI0 SCK      | SCLK     |
| GP19      | SPI0 TX (MOSI)| MOSI     |
| GP20      | INT (선택)    | INT      |
| GP21      | RST (선택)    | RST      |
| 3V3       | 전원          | VCC      |
| GND       | GND           | GND      |

### STS3215 시리얼 버스 (Half-duplex UART)
| Pico 2 핀 | 기능                  | 비고 |
|-----------|-----------------------|------|
| GP0 (TX)  | UART0 TX              | 방향 제어와 함께 사용 |
| GP1 (RX)  | UART0 RX              | |
| GP2       | DE/RE (방향 제어)     | MAX485 또는 직접 트랜지스터 회로 |
| 3V3/5V    | 로직 전원             | 레벨에 맞게 |
| GND       | GND                   | |

> STS3215는 기본적으로 TTL Half-duplex이다.  
> 안정성을 위해 MAX485 칩을 넣고 RS485로 변환해도 되고, 단순하게 TX/RX를 다이오드+저항으로 합쳐도 된다.

### PWM 4축
| Pico 2 핀 | 기능     | 비고 |
|-----------|----------|------|
| GP6       | PWM0     | Slice 3A |
| GP7       | PWM1     | Slice 3B |
| GP8       | PWM2     | Slice 4A |
| GP9       | PWM3     | Slice 4B |

### 기타
| Pico 2 핀 | 기능          |
|-----------|---------------|
| GP25      | 온보드 LED (상태 표시) |
| RUN       | 리셋          |
| USB       | 펌웨어 업로드 / 디버그 |

---

## 4. BOM (주요 부품)

| 품목                    | 수량 | 대략 가격 (원화) | 비고 |
|-------------------------|------|------------------|------|
| Raspberry Pi Pico 2     | 1    | 7,000~9,000     | RP2350 |
| W5500 이더넷 모듈       | 1    | 5,000~8,000     | SPI 버전 권장 |
| STS3215 서보            | 6    | -               | 기존 사용 중인 것 |
| MAX485 (또는 SN75176)   | 1    | 500~1,000       | Half-duplex 방향 제어용 (선택) |
| 3.3V 레귤레이터 (AMS1117 등) | 1 | 300             | W5500/Pico 안정 전원 |
| 전해 커패시터 100~470uF | 2~3  | 소액            | 전원 디커플링 |
| 세라믹 0.1uF            | 여러 | 소액            | |
| 커넥터 (JST-PH, XH 등)  | 적절 | -               | 서보/전원/이더넷 |
| 2레이어 쓰루홀 PCB      | 1    | -               | 직접 제작 |

**총 추가 비용 (보드 기준)**: 약 15,000~20,000원 내외 (서보 제외)

---

## 5. 간단한 동작 흐름

1. 상위 보드가 100Hz로 UDP 패킷 전송
2. Pico 2가 W5500으로 패킷 수신
3. CRC 검사 후 Sequence 확인
4. 6개 Position 값을 Feetech Sync Write 패킷으로 변환하여 UART로 전송
5. 4개 PWM 값을 해당 핀에 duty cycle 반영
6. (선택) 주기적으로 현재 위치/상태를 상위 보드로 피드백

---

## 6. 참고 사항

- Pico 2 클럭은 기본 150MHz로 충분하다.
- 실시간성을 높이려면 Core 0 = 통신, Core 1 = 모터 제어로 분리하는 것을 추천.
- STS3215 기본 보드레이트는 1,000,000 bps로 맞출 것.
- 전원은 서보 쪽과 로직 쪽을 분리하고, 공통 GND만 연결하는 것이 안전하다.
